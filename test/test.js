var chai = require("chai");

var assert = chai.assert;

describe('add', function() {
  it('simple test is array empty', function() {
    var arr = [];
   
    
    assert.equal(arr.length, 0);
  });
});